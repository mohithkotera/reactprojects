import React from "react";

const Setting = () => {
  return (
    <div
      style={{
        width: "100%",
        alignItems: "center",
        justifyContent: "center",
        display: "flex",

        position: "relative",
        top: "23%",
      }}
    >
      <h1 style={{ fontSize: 90, fontWeight: 700, color: "#e9ecef" }}>
        Setting
      </h1>
    </div>
  );
};

export default Setting;
